import { ContenteditableModel } from './contenteditable-model.directive';

describe('ContenteditableModelDirective', () => {
  it('should create an instance', () => {
    const directive = new ContenteditableModel();
    expect(directive).toBeTruthy();
  });
});
