import { FormsDirective } from './forms.directive';

describe('FormsDirective', () => {
  it('should create an instance', () => {
    const directive = new FormsDirective();
    expect(directive).toBeTruthy();
  });
});
